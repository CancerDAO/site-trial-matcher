# 当前 Demo 验证报告

## 数据库

- 活跃数据库：`data/demo_recent.db`
- WHO MCP 数据时间：2026-08-18 16:59 UTC
- MCP 当前可稳定取得的第一页：100 项中国、招募中、干预性癌症试验；100 项详情全部成功，0 失败。
- 合并用户提供的 ChiCTR XML 1 项后：101 项试验、81 个申办/合作机构、203 个注册号、4108 条入排标准。
- 100 项 MCP 试验均可由 China/中国国家证据纳入，其中主注册号不一定是 ChiCTR。

服务端第 2 页搜索在本次运行中间歇性返回 `unable to open database file`。客户端现在会保留第一页和完整详情，并在快照中写入 `selection_complete=false` 与 `pagination_warning`，不会把部分数据伪称为完整选择。患者匹配本身只访问本地 SQLite，不受该延迟影响。

## 单患者端到端基准

患者：56 岁女性、转移性肺腺癌/非小细胞肺癌、EGFR L858R、ECOG 1、既往奥希替尼。

| 阶段 | 结果 | 耗时 |
|---|---:|---:|
| 本地全量确定性预筛 | 101 项；排除 60，送模型 41 | 150.6 ms |
| MiniMax 首轮（20 批、16 并发） | 18 批通过、2 批被契约拒绝 | 97.4 s |
| 断点续跑 2 个失败批次 | 全部通过 | 123.9 s |
| 模型总主动运行 | 41 项全部覆盖 | 221.3 s（3.69 分） |
| 最终结果 | 潜在 12，已排除 89 | 远低于 20 分钟 |

最终潜在试验中合作 1 项、非合作 11 项。模型新增排除 29 项，所有排除都通过了试验原文、患者非空字段、排除类型和最低置信度校验。两次运行时间相加是保守口径；断点续跑不会重复已经通过的 18 个批次。

## 测试

19 项自动化测试全部通过，覆盖 XML、模板、MCP 快照、非 ChiCTR 中国试验、NSCLC/SCLC 边界、确定性入排、模型证据绑定、断点合并和批量输出。
